# Legacy upstream source

The original upstream project is preserved on the Git branch named `legacy`.

The supported maintained release is on `main`. The `main` branch starts with a new root commit so experimental development commits are not part of the maintained history.

Use GitHub’s branch selector to switch between `main` and `legacy`.
